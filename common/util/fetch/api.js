const baseUrl = `/api/`;

// 获取属性列表
export const propertyList = baseUrl + 'propertyList';

// 添加新属性
export const postProperty = baseUrl + 'property';

// 获取bundles列表
export const getBundles = baseUrl + 'getBundles';

// 增加新bundle
export const postBundle = baseUrl + 'bundle';

// bundle上传
export const bundleUpload = baseUrl + 'bundleUpload';

// 更新bundle
export const updateBundle =  baseUrl + 'updateBundle';

// 获取用户信息
export const getUser = baseUrl + 'getUser';

// 注册新用户
export const reg  = baseUrl + 'reg';
